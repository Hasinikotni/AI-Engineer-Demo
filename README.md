# AI Engineer - Web Data Extraction and Entity Resolution Pipeline
A Python-based web data extraction pipeline that crawls webpages, extracts useful information and entities, performs entity resolution, and saves structured results in JSON format.
## How to Run
## Input and Output
Output: data/pages.json and data/entities.json
Run: python -m src.batch_pipeline
Input: data/urls.txt
